/*If marks obtained by a student in 5 different subjects are input from keyboard, find
out the aggregate marks and percentage marks obtained by the student.*/
#include<stdio.h>
#include<math.h>
int main()
{
   int a,b,c;
   prinf("enter the value of a:");
   scanf("%d",&a);
   prinf("enter the value of b:");
   scanf("%d",&b);
   a=c;
   a=b;
   b=c;
    prinf("enter the value of a:%d",a);

    return 0;
}

